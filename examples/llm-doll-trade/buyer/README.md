# buyer/ — 买方模型配置

M11 演示中买方（`agent_buyer`）的数据目录。

- `config.json` — 身份、邮箱、模型角色说明。
- `wishlist.json` — 需求：豪华套装（`cotton-doll-deluxe-set`）×1，预算 ≤ 150.00 CNY，验收条件 3 条。

身份种子不在此处复制：演示运行时从权威源
`protocol/test-vectors/vectors.json#identities.agent_buyer` 读取。
